# Data Directory

Place your documents (PDF, TXT, or Markdown files) in this directory for ingestion.

Supported formats:
- `.pdf` - PDF documents
- `.txt` - Plain text files
- `.md` - Markdown files

The ingestion script will automatically process all supported files in this directory and its subdirectories.
